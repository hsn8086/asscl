name: domain
description: Domain layer - entities, enums, repository interfaces, use cases.
version: 1.0.0
publish_to: 'none'

environment:
  sdk: ^3.11.1

dependencies:
  equatable: ^2.0.7
  uuid: ^4.5.1

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
  mockito: ^5.4.6
  build_runner: ^2.4.15
