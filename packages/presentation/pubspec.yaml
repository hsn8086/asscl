name: presentation
description: Shared widgets, themes, and design tokens.
version: 0.0.1
publish_to: 'none'

environment:
  sdk: ^3.11.1
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter
  domain:
    path: ../domain

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
